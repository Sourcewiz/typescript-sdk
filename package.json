{
  "name": "wizcommerce",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --emitDeclarationOnly --declaration --outDir dist && npx esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm",
    "prepare": "npm run build"
  },
  "keywords": ["wizcommerce", "sdk", "api"],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.0.14",
    "typescript": "^5.8.3"
  }
}
